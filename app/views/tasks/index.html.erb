
<!-----TESING AREA------>
<!--Idea: try changing the color based on the user's name (all users names are colors)-->
<!--<br>
<div style="background-color: #008000;">
  <div style= "color: #e6ffee;">
    ------------TESTING AREA-----------
    <br>
    Current user Id= <%#= current_user.id%>
    <br>
    Current User Name = <%#= current_user.first_name%>
  </div>

</div > -->
<!---------------------->

<p id="notice"><%= notice %></p>

<h1>Tasks</h1>

<table>
  <thead>
    <tr>
      <!--<th>User_id</th>-->
      <th>Title</th>
      <th>Description</th>
      <th>Complete</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <!--<td><%#= task.user.id%></td>-->      
        <td><%= task.title %></td>
        <td><%= task.description %></td>
        <td><%= task.complete %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>
